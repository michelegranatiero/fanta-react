import { createContext } from "react";

/* export const SettingsCtx = createContext(null);

export const PlayersCtx = createContext(null);

export const SelPlayerCtx = createContext(null);

export const TeamsCtx = createContext(null);
 */

export const SharedContext = createContext(null);